# ENGINE_VALIDATION.md

Generated by `scripts/t3_50_reports.py` from machine-readable Phase 3
artifacts. Pre-registration: `PHASE3_PREREGISTRATION.md`
(sha256 `064ce1fb9939b10d3b22be1f74aa3da28f487ec3917449db0794cf6431af5a63`,
committed `9ca09e9b950d4c473af6ff8740f8b3d1625d1a2b`), frozen **before** any governed symbolic run.

Master plan 10: verify installation, the Julia runtime, deterministic seeding as
far as the library permits, checkpointability, operator definitions, complexity
scoring, and a trivial smoke test on a known synthetic equation.

**Successful installation is not scientific validation.** This file records only
that the engine does what the protocol assumes it does.

## Environment

| Component | Version |
|---|---|
| PySR | **1.5.10** |
| SymbolicRegression.jl | 1.11.3 |
| Julia | 1.12.6 |
| gplearn | 0.4.3 |
| SymPy | 1.14.0 |
| NumPy / pandas | 2.5.2 / 3.0.5 |
| scikit-learn / SciPy | 1.9.0 / 1.18.0 |
| Python | 3.13.12 |
| Platform | Darwin 25.1.0 arm64, 8 cores |

## Smoke test

Planted: `1.5*x0 + 0.5*x1**2` on clean data.

| Quantity | Value |
|---|---|
| Pareto front size | 6 |
| Wall time | 5.91 s |
| **Recovered** | **True** |
| Recovered expression | `((x0 + square(x1)) / 2.0000467) + x0` |
| Recovered complexity | 8 |
| Symbolically equivalent | False |
| Best validation R² on the front | 1.000000 |

## Determinism

| Check | Result |
|---|---|
| Two runs at the same seed produce an identical Pareto front | **True** |
| Equations on that front | 9 |
| A different seed produces a different front | True |

PySR is run with deterministic=True and parallelism='serial'; reproducibility is asserted at that setting, which is the one every governed run uses.

## Operator and complexity definitions

Binary ['+', '-', '*', '/'], unary ['sqrt', 'log', 'square', 'cube', 'inv'], maximum complexity
20. Excluded: exp, trigonometric functions, free-exponent ^.

Complexity worked examples, showing the two engines share one scale:

| Expression | Complexity |
|---|---|
| `a` | 1 |
| `a + b` | 3 |
| `sqrt(a)` | 2 |
| `a**2` | 2 |
| `square(a)` | 2 |
| `sqrt(a*b)` | 4 |
| `a*b + 1.0` | 5 |

## Checkpointability

| Property | Result |
|---|---|
| Unit | `(block, world, seed)` |
| Resume skips completed units | True |
| Resume advances to the next incomplete unit | True |
| A truncated file counts as incomplete, not as a result | True |
| Write | tmp file then os.replace |

successful installation is not scientific validation; this file records only that the engine behaves as the protocol assumes.
