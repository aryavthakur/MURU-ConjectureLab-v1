"""Phase 3 synthetic ground-truth worlds.

`truth` holds the planted equations. Nothing under `muru.discovery` may import
it, directly or transitively; `tests/test_p3_import_graph.py` enforces that by
walking the import closure.
"""
