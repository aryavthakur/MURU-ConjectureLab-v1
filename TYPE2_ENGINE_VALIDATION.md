# TYPE2_ENGINE_VALIDATION.md

Generated by `scripts/ov_50_reports.py` from machine-readable
artifacts of the prospective objective-alignment validation study.
Pre-registration: `TYPE2_VALIDATION_PREREGISTRATION.md` (sha256 `9ce046294940464343cd74978931fa3fc9ed7bb79541484a9fba7b8c142ee9e7`,
committed `307e4e083256b04ee7c4452282f06c06812785b8`), frozen **before** any fresh validation world was
generated. This study does not modify Phase 3, whose verdict remains
`STOP BEFORE PHASE 4`.

Master plan §10: verify installation, the Julia runtime, deterministic seeding as
far as the library permits, checkpointability, operator definitions, complexity
scoring, and a smoke test on a known equation. This file additionally checks the
two properties the Type 2 machinery adds.

**Successful installation is not scientific validation.** This file records only
that the engine does what the protocol assumes it does.

## Environment

| Component | Version |
|---|---|
| PySR | **1.5.10** |
| SymbolicRegression.jl | 1.11.3 |
| Julia | 1.12.6 |
| gplearn (comparison arm) | 0.4.3 |
| SymPy | 1.14.0 |
| NumPy / pandas | 2.5.2 / 3.0.5 |
| scikit-learn / SciPy | 1.9.0 / 1.18.0 |
| Python | 3.13.12 |
| Platform | Darwin 25.1.0 arm64, 8 cores |

## Smoke test

Planted: `1.5*x0 + 0.5*x1**2` on clean data.

| Quantity | Value |
|---|---|
| Pareto front size | 13 |
| Wall time | 10.72 s |
| **Recovered** | **True** |
| Recovered expression | `(0.25114536 / x1) + ((x0 * 1.494898) - ((x1 * -1.3713758) + 1.0988066))` |
| Recovered complexity | 14 |
| Symbolically equivalent | False |
| Best validation R² on the front | 0.998927 |

## Determinism

| Check | Result |
|---|---|
| Two runs at the same seed produce an identical Pareto front | **True** |
| A different seed produces a different front | True |

PySR runs with `deterministic=True` and `parallelism='serial'`; reproducibility
is asserted at the setting every governed run uses.

## Type 2 additions

| Check | Result |
|---|---|
| The cached-compile evaluator agrees bit-for-bit with `grammar.evaluate` | **True** |
| The mass-block exponent is invariant to which size proxy is written | **True** |

| Expression | Mass-block exponent |
|---|---|
| `sqrt(precursor_mz)` | 0.5000 |
| `sqrt(total_atom_count)` | 0.5000 |
| `sqrt(rdbe)` | 0.5000 |

## Complexity, one metric everywhere

| Expression | Complexity |
|---|---|
| `a` | 1 |
| `a + b` | 3 |
| `sqrt(a)` | 2 |
| `a**2` | 2 |
| `sqrt(a*b)` | 4 |
| `a*b + 1.0` | 5 |

Protected numerics: `1/(a-a)` and `log(a-b)` on an in-domain matrix are marked
invalid rather than raising or scoring
({"1/(a-a)": {"invalid_fraction": 1.0}, "log(a-b)": {"invalid_fraction": 1.0}}). Invalidity can never improve a score.

## Checkpointability

| Property | Result |
|---|---|
| Unit | `(block, world, seed)` |
| Resume skips completed units | True |
| A truncated file counts as incomplete, not as a result | True |
| Write | tmp file then os.replace |
