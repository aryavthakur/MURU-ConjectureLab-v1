# MURU v2 Reconciliation — Hostile Review

**Status:** Complete. 4 independent adversarial subagents (workflow run `wf_c7055e68-973`), each assigned a disjoint subset of the mission's anti-pattern checklist, reviewed the six drafted synthesis documents against the underlying 8 evidence dossiers and, where warranted, primary source (production code, git history, source markdown) directly — not merely against each other's prose. **17 findings** were returned (7 MATERIAL, 8 MINOR, plus 2 findings folded into others); **11 clean checks** were returned with shown work. Every MATERIAL finding and every MINOR finding judged actionable without new evidence has been **applied** to the six deliverables before this document was finalized — this is not a list of open TODOs, it is a record of what was found and what was done about it.

Method note: findings are numbered `HR-01` through `HR-17` in the order the four agents' outputs were processed. Each carries the reviewing agent's own severity judgment (not softened), the fix applied (or the reason none was needed/possible), and the reviewer's own disclosed remaining uncertainty verbatim.

---

## MATERIAL findings

### HR-01 — Repair-space-closure summary count was internally inconsistent (double-counted set-valued retention + 3 nested-classification rows)
**Found by:** hr-retention-e2boundary-stressworlds, independently corroborated in its arithmetic by hr-cross-document-coherence.
**Finding:** `MURU_V2_REPAIR_SPACE_CLOSURE.md`'s table classified "set-valued retention" as `WAITING_ON_E2` (line 27), but the summary section separately re-counted it under `REMAINS_LICENSED_FOR_TEST` with a bracketed "[conditionally]" hedge not present in the table itself — a genuine double-count that inflated the headline "5 remain licensed" claim. A second, independent audit of the same section found the printed tally (10/4/2/5/2/4/1=28) did not match either the actual row count (25) or the document's own prose ("~24"), traced to three further rows (M1 statistic redesign, generic reparameterization, F05 bound changes) each being informally counted toward two classification buckets in the surrounding prose without disclosure.
**Severity:** MATERIAL.
**Resolution applied:** Recomputed the tally with one classification per row (verified: 12 `CLOSED_NEGATIVE` + 2 `CLOSED_THEORETICALLY_IMPOSSIBLE` + 1 `CONTRA_INDICATED` + 4 `REMAINS_LICENSED_FOR_TEST` + 2 `WAITING_ON_E2` + 3 `GOVERNANCE_ONLY` + 1 `OPTIONAL_PAPER_CHARACTERIZATION` = 25, matching the row count exactly), added an explicit single-classification-per-row methodology note, corrected the headline to "4 unconditionally licensed... plus 2 further classes gated on E2a's seal," and propagated the corrected framing into `MURU_V2_FINAL_REMAINING_PROGRAM.md`'s own intro sentence.
**Remaining uncertainty (reviewer's own words):** "It is plausible the author intended the bracket to signal a genuine partial/soft classification... rather than an oversight... this looks like a localized synthesis slip confined to this one summary section rather than a pervasive misrepresentation." No further uncertainty after the fix — the corrected count is arithmetically closed and independently reproducible from the table alone.

### HR-02 — Governance Amendment doc omitted a required pre-freeze check the reconciliation doc itself names
**Found by:** hr-cross-document-coherence.
**Finding:** `MURU_V2_MASTER_MATHEMATICAL_RECONCILIATION.md` Part XI lists exactly 4 "Required before freeze" items, including "confirm every dependent repair-lane's own preregistration is compatible with the single-shot E6-S/E6-R re-scoping" (traces to `L-E6-05`). `MURU_V2_PRE_E6_GOVERNANCE_AMENDMENT.md`'s 9 amendments had no corresponding entry, and its closing summary table implied exhaustive coverage ("everything else: NO_CHANGE") — a reader relying on the amendment doc as the authoritative pre-freeze checklist would miss this item entirely.
**Severity:** MATERIAL.
**Resolution applied:** Added **Amendment 10**, naming `L-E6-05` explicitly, specifying the starting point for the check (the retention-remediation preregistration's own E6-discharge clause), and updating the summary table.
**Remaining uncertainty:** None — this was a pure omission with a clear, already-specified fix; the check itself remains to be performed by whoever owns the E6 freeze process, which is correctly outside this reconciliation's own authority to perform.

### HR-03 — Part VIII undercounted the framework's blocking disambiguations as "two" when the ledger and the source document both say "three"
**Found by:** hr-cross-document-coherence.
**Finding:** `MURU_V2_MASTER_MATHEMATICAL_RECONCILIATION.md` Part VIII's closing sentence said "two numbers... blocking," omitting the Dimension A kappa-clause tension (`L-REC-03`) entirely from Part VIII/XI — even though the evidence ledger's own cross-cutting note names three, the frozen recoverability-ceiling source document itself states "3 blocking disambiguations," and the governance amendment devotes a full entry (Amendment 6) to the kappa clause, calling it "the highest-leverage unresolved item in the entire framework." A reader of the Mathematical Reconciliation document alone — the document titled to synthesize exactly this — would not learn a third, allegedly highest-stakes ambiguity exists.
**Severity:** MATERIAL.
**Resolution applied:** Rewrote the closing of Part VIII to enumerate all three items explicitly (DA5, the scale convention, and the kappa clause, with the kappa clause's mechanism and stakes described in full), matching the ledger's and the source document's own count.
**Remaining uncertainty:** None remaining after the fix; the reviewer's own hedge ("possible the author considered the kappa clause categorically different... but even under that reading, the complete absence... is not explained") is resolved by no longer having any absence to explain.

### HR-04 — `L-REC-04` overstated the CRC hostile audit's resolution status
**Found by:** hr-cross-document-coherence.
**Finding:** The ledger claimed all 5 CRC hostile-audit findings were "fixed and re-verified." Cross-checked against the source dossier: only the 1 MATERIAL finding (and 1 of 4 MINOR findings) was actually corrected in the post-audit commit; the source's own language for the other two MINOR findings is "the audit explicitly could not explain" (M3) and "unsupported by any baseline tried" (M5) — disclosed, standing discrepancies, not corrections. None change F05's FAILED disposition, but the blanket "all fixed" claim was inaccurate.
**Severity:** MATERIAL.
**Resolution applied:** Rewrote `L-REC-04` to distinguish which findings were corrected from which remain disclosed-but-unresolved, using the source's own language.
**Remaining uncertainty (reviewer's own words):** "I have not independently read the CRC hostile-audit document itself... it is possible the audit's own prose elsewhere describes M3/M5 as resolved in some sense not captured in the subagent's terse summary" — noted; the fix uses the more conservative (disclosed-not-fixed) reading pending any such further evidence, which is the correct default under the mission's own "do not weaken the audit" instruction.

### HR-05 — Dimension G omitted as a second instance of the measure-substitution pattern (found independently by two separate hostile-review agents)
**Found by:** hr-denominator-measure **and** hr-m1m2m3-mechanism, independently, with near-identical reasoning — the strongest corroboration in this review.
**Finding:** `L-COR-01` and the corresponding Part VIII paragraph named only one instance (Dimension I) of the frozen framework substituting `REFERENCE_FUNCTION_SPACE_DISTANCE` for `OBSERVATIONAL_DISTINGUISHABILITY_UNDER_NOISE`, even though the source dossier's own table named a second, independent instance: Dimension G's ambiguity trigger ORs together a noise-conditional statistic (`ΔBIC≤2.0`) and a zero-noise structural statistic (`RelRMSE≤0.02`) undifferentiated — and this is the actual mechanism feeding `SET_VALUED_ACCEPTABLE_RECOVERY_CEILING`'s ambiguity-credit route, one of the four headline denominators.
**Severity:** MATERIAL (two independent agents converging on the same omission of a load-bearing mechanism raises this above a routine miss).
**Resolution applied:** Added Dimension G as a second, explicit instance in both `L-COR-01` and Part VIII of the reconciliation doc, with its consequence for `SET_VALUED_ACCEPTABLE_RECOVERY_CEILING` named directly.
**Remaining uncertainty (reviewer's own words, both agents converged on this caveat):** neither agent independently re-opened `MURU_V2_CORRECTNESS_CONFIDENCE_PROTOCOL.md` itself to re-verify the exact Dimension G predicate text — both relied on the source dossier's own claim to have verified it against the PROTOCOL directly. This is a second-order provenance gap (a hostile review trusting a Stage-A agent's citation rather than re-deriving it from primary source) that this document discloses rather than hides.

### HR-06 — A real factual error: Part IV claimed no descriptor-blind M1 candidate exceeded "~10%" power, when the ledger it cites two sentences earlier says the correct figure is 0.0%
**Found by:** hr-m1m2m3-mechanism.
**Finding:** `MURU_V2_MASTER_MATHEMATICAL_RECONCILIATION.md` Part IV item 6 stated "no genuinely descriptor-blind candidate... has ever exceeded ~10% power at the reference cell," directly inconsistent with `L-M1-03` (cited two sentences earlier in the same paragraph), which correctly states S1/S2/S3/S5 measure "exactly 0.0%" at the reference cell. The reviewer traced the likely origin: S1/S5's *full-grid-averaged* power (10.5%) is dominated by a disclosed noise=0.0 degeneracy artifact unrelated to real detection signal, and was apparently substituted for the reference-cell figure during synthesis.
**Severity:** MATERIAL — this is the one finding in the entire review that is a genuine numeric error introduced during synthesis, not a source-evidence gap.
**Resolution applied:** Corrected the sentence to state the true reference-cell value (0.0%, matching `L-M1-03` exactly), added a note explaining why the grid-averaged 10.5% figure must not be read as a reference-cell number, and cited the exact file:line verification (`scripts/m1r_common.py:48`, `scripts/m1r_fit.py:172,311`) directly in the corrected text.
**Remaining uncertainty:** None on the corrected number itself (independently re-verified via `git show ec3a974` by a second reviewer in a separate finding, HR-13 below). The reviewer's own hedge ("I inferred the likely numeric origin... via a plausible textual match, not a citation the reconciliation doc itself supplies") concerns only *why* the error occurred, not *whether* it was an error — that part is unambiguous.

### HR-07 — Causal-architecture doc's "Explicitly NOT the cause" line for M3 over-scoped a narrow finding into an implied broad one
**Found by:** hr-m1m2m3-mechanism.
**Finding:** `MURU_V2_MASTER_CAUSAL_ARCHITECTURE.md`'s M3 row listed "generic Phi-estimation quality" as "Explicitly NOT the cause," citing only `L-M3-04` (the impossibility theorem, which is scoped specifically to the *parameter-bias floor*). This omitted `L-M3-06` (SA1's finding that oracle-clean-training Phi recovers M3's *detector/LOEO* power from 1.3% to 90.7% — the single largest recovery effect found anywhere in the M3 investigation), risking the false impression that Phi quality is irrelevant to M3's failure overall, when in fact it is irrelevant only to the specific floor mechanism the impossibility theorem forecloses.
**Severity:** MATERIAL — this is exactly the "assuming M3 parameter repair implies/doesn't imply detector repair" anti-pattern the mission explicitly asked this review to hunt for, found in the opposite direction (a real detector-power lever mislabeled as inert).
**Resolution applied:** Rewrote the M3 causal-architecture section to scope "Explicitly NOT the cause" precisely to the parameter-bias floor, and added an explicit paragraph distinguishing "cannot fix the floor" from "irrelevant to detector power," citing `L-M3-06`'s 1.3%→90.7% figure directly, while preserving the correct conclusion that a 4th profile-estimator-architecture study remains `CONTRA_INDICATED` (it targets the floor, not the live detector-power lever).
**Remaining uncertainty (reviewer's own words):** "It is possible the author intended 'Explicitly NOT the cause' to be read narrowly... but this scoping is not stated in the line itself" — resolved by the fix now stating the scoping explicitly.

### HR-08 — M3's causal-architecture classification (`FUNCTIONAL_EQUIVALENCE_LIMIT`) was a category force-fit, propped up by an undocumented parenthetical redefinition
**Found by:** hr-m1m2m3-mechanism.
**Finding:** The original classification hedged "(most precisely: a normalization/estimand-definition limit — the closest listed category)" — effectively inventing an unlisted category and filing it under an existing name. The reviewer argued `ESTIMATOR_LIMIT` (already used as a *secondary* contributor one line below, for a related mechanism) is the better-fitting existing category, because the floor mechanism is escapable by redesigning the estimator/estimand (per the Phi-bias theory's own relative-plateau construction), unlike M1's Theorem-1 unidentifiability, which is not fixable by any statistic or estimator whatsoever — the exact distinction that should drive `BENCHMARK_IDENTIFIABILITY_LIMIT` vs. `ESTIMATOR_LIMIT` in this taxonomy. The reviewer also noted this is not cosmetic: the document's own cross-cutting summary count depends on this classification.
**Severity:** MATERIAL (explicitly noted by the reviewer to be load-bearing for the document's own headline structural claim).
**Resolution applied:** Reclassified M3's PRIMARY as `ESTIMATOR_LIMIT`, removed the parenthetical redefinition, folded the Fisher condition-ratio penalty in as a related secondary `ESTIMATOR_LIMIT` mechanism, and recomputed the cross-cutting summary honestly: **seven** of nine items (not six) have primary cause outside raw estimator/search quality; **two** items (M3 and symbolic equivalence) are estimator/implementation-level, but the document now states explicitly that these two are not alike in kind — M3's is a structural design floor fixable only by estimand redesign, symbolic equivalence's is a straightforward patchable bug.
**Remaining uncertainty (reviewer's own words):** "Category-taxonomy judgment calls are inherently somewhat subjective... this may be a deliberate, accepted simplification rather than an oversight." Accepted as a genuine judgment call; the fix is the reviewer's own proposed resolution (b), applied in full rather than partially.

---

## MINOR findings (applied)

### HR-09 — `L-M1-07`'s tag `[TENSION, resolved]` contradicted both the ledger's own legend and its own prose
**Severity:** MATERIAL by strict legend-consistency (the tag literally means the opposite of what the entry's own text says), MINOR in practical consequence (no downstream claim relied on the mistagging). **Fixed**: retagged `[TENSION, unresolved]`, with an explanatory note distinguishing it from the (correctly resolved) S4-vs-theory question in the same neighborhood.

### HR-10 — `L-M3-03`'s tag `[SUPERS]` did not match its own content (nothing is being superseded; it is the theory's own disclosed, currently-unexplained gap against its own idealization)
**Severity:** MINOR (reviewer: "I did not find any downstream claim... that relies on L-M3-03 actually being 'superseded' rather than 'unresolved'"). **Fixed**: retagged `[TENSION, unresolved]`.

### HR-11 — Positive-scale-invariance discussion silently omitted that SIM's own Dimension A distance is a *bounded*, not fully unconstrained, scale quotient
**Severity:** MINOR (does not change the Dimension E recommendation; the omission just meant the "every other mechanism already does X" list was inexact about one entry). **Fixed**: added an explicit clarifying parenthetical in Part VIII distinguishing Dimension A's box-constrained quotient from the fully-unconstrained convention, and noting AMCS-v2's 0.14–0.84% closeness check was validated on descriptor-only pairs, not (yet) on the interaction pairs where DA5's swing actually lives — the reviewer's own residual uncertainty, preserved rather than papered over.

### HR-12 — F07's `parameter_recovery` ceiling of 1.000 lacked a safety-trap-style caveat analogous to F18's
**Severity:** MINOR (F07's metric is never folded into a headline denominator, unlike F18's, which meaningfully limits the practical risk; reviewer notes this reduces but does not eliminate the hazard, and that this finding is the reviewer's own extrapolation of the pattern rather than something the source dossier explicitly flagged). **Fixed**: added a parenthetical to the F07 rows in both the ledger and Part VIII stating the mechanism explicitly and warning against citing it as recovery evidence.

### HR-13 — Independent re-verification (clean, but recorded as a finding-adjacent check): S4's hard-coded construction
Re-verified directly via `git show ec3a974` (not trusting any JSON citation) that `scripts/m1r_common.py:48` and `scripts/m1r_fit.py:172,311` do exactly what the ledger claims. **No fix needed — this strengthens, not weakens, the existing text**; the exact commit/line citations from this independent re-verification were folded into the Part IV correction (HR-06) as additional grounding.

### HR-14 — DA5's evidentiary basis was narrower in the ledger than in its own source (a second near-miss pairwise distance, `aff/int` at 2.13%, was dropped)
**Severity:** MATERIAL for completeness of the DA5 picture (a second near-cutoff distance makes the measure-dependence look systematic rather than a single-pair artifact), MINOR for any conclusion (does not change DA5's blocking status or any ceiling number). **Fixed**: added the `aff/int` figure to `L-REC-02` and to Part VIII's family table, with the reviewer's own hedge preserved ("essentially on the cutoff... a genuine near-tie rather than a second clear violation").

### HR-15 — "Proof" language used for a `NUMDER`-tagged (not exact) result in Part V
**Severity:** MINOR (informal use of "proof" in prose; the underlying qualitative conclusion is independently supported by a genuinely exact identity, P6, discussed two paragraphs earlier). **Fixed**: replaced "proof" with "strong evidence," and pointed the sentence explicitly at the exact P6 mechanism underneath the pooled-power numbers rather than at the numbers themselves.

### HR-16 — Cross-cutting note 1 in the ledger read as an exhaustive "three tensions" claim while two further `[TENSION]`-tagged M2 entries exist elsewhere in the same ledger
**Severity:** MINOR (the two additional tensions, `L-M2-10`/`L-M2-11`, are lower-consequence and already correctly tagged in their own section — only the cross-cutting note's *framing* was too broad). **Fixed**: scoped the note's wording explicitly to "denominator/E6-scoped" tensions and named the two additional M2-internal tensions alongside it so a reader is not misled into thinking the ledger has only three `[TENSION]` entries total.

---

## Clean checks (verified, no finding — shown work preserved)

The following anti-patterns were genuinely hunted for and **not found**, with the reviewing agent's own verification method preserved for auditability:

- **Circular denominator definitions**: every formula in Part VIII traced to `registry.py`/`generator.py`/SIM/E3/E-STAB; the one dependency on MURU-authored code (`g_hat` via the frozen, non-search `rc5_estimate.py`) is explicitly and correctly disclosed as a weaker, different claim than "no MURU code dependency at all."
- **Double exclusion**: population arithmetic for all four ceilings traced and found non-overlapping; F07's dual-axis exclusion (fails Dimension B, separately clears Dimension A) is a single exclusion via one sufficient condition, not two independent subtractions.
- **Post hoc favorable metric selection**: Amendments 4–6's recommendations are each grounded in stated, checkable textual-consistency arguments, with the ceiling-raising consequence explicitly disclosed as a *consequence*, not the *justification*; DA5 itself is left genuinely unresolved with neither reading favored.
- **MURU manipulating its own denominator**: Deviation D-1's `Adq*` predicate independently re-verified to depend only on planted/generator quantities; every Part VIII denominator population is fixed before any MURU run, with only numerators depending on MURU's actual output.
- **Stress-world leakage (F05)**: every constructed-regime number in all six documents is labeled as such; Part X's own text pre-empts the exact misreading this anti-pattern targets.
- **Denominators/endpoints depending on live search/PySR output**: independently verified by direct source inspection (`rc5_estimate.py`, `discovery/estimate.py`) that neither imports `pysr` or `gplearn`.
- **Claims depending on unfinished E2**: `E2_BLINDING_LAYER_DOCUMENTATION.md` read directly and independently confirmed outcome-blind as characterized; every E2-interim-derived number traced to its underlying JSON and confirmed correctly hedged as directional/non-replicating everywhere it appears.
- **E2-boundary-violation count**: independently regex-verified against all 8 (not just a sample of 4) Stage-A dossiers' `files_read` arrays — 0 matches, confirming the provenance JSON's own count.
- **Provenance JSON's numeric claims** (116 files, 73 ledger entries, 22 open conflicts, 38 warnings): independently recomputed from the 8 raw dossiers by a second, separate agent — exact match.
- **E6 freeze-status consistency**: the ledger, the governance amendment, and the reconciliation doc's Part XI were compared word-for-word and found to converge on the same facts and the same severity, with no document over- or under-stating relative to the others.
- **`FINAL_REMAINING_PROGRAM` proposing anything already `CLOSED_NEGATIVE`/`CLOSED_THEORETICALLY_IMPOSSIBLE`**: every arm of every proposed experiment checked against the repair-space-closure table; no violation.

---

## What this hostile review did *not* find, despite genuinely looking

No `BLOCKING`-severity finding was returned by any of the four agents — nothing that would require reversing a headline conclusion (M1's identifiability limit, M2's decision-rule limit, M3's estimator-floor limit after correction, F05's adversarial-only scope, F09's retention classification, E6's uncommitted status, or any of the three blocking denominator disambiguations) was challenged as factually wrong. The 7 MATERIAL findings were entirely synthesis-layer defects — an arithmetic double-count, an omitted cross-reference, an overstated audit-resolution claim, a genuine but narrow numeric transcription error, a dropped table row from a source dossier (found independently twice), a mis-scoped causal claim, and a category-taxonomy force-fit — not defects in the underlying evidence base itself, which the 8 Stage-A dossiers had already extensively self-audited (73 ledger entries, 22 open conflicts, 38 warnings, all independently re-verifiable). This is the expected and correct outcome of a synthesis-then-adversarial-review pipeline: the raw evidence gathering was rigorous enough that the hostile review's real work was catching synthesis-introduced errors, which it did, at real (non-trivial) rate — 17 findings across ~2,400 lines of drafted markdown is not a rubber stamp.
